{
  "name": "task-5 AI Assessment",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "context": "..",
  "workspaceFolder": "/app",
  "extensions": [
    "ms-python.python",
    "ms-toolsai.jupyter"
  ],
  "settings": {
    "python.pythonPath": "/usr/local/bin/python"
  },
  "postCreateCommand": "pip install --no-cache-dir -r requirements.txt"
}
